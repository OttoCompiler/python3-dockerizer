# python3 fast dockerizer

run with `python3 dockerizer.py <your_entrypoint.py>` for simplest use. Toolchain will automatically figure out your dependencies and handle their installation for you.

 Provided as-is. I accept no liability for the use or misuse of this software.
